<h1>Restaurants#index</h1>

<ul>
<% @restaurants.each do |restaurant|%>
<li> <%= link_to restaurant.name, restaurant_path(restaurant) %> ( <%= link_to "Edit", edit_restaurant_path(restaurant) %> - <%= link_to "Delete", restaurant_path(restaurant), method: 'delete' %> )</li>
<%end%>
</ul>

<%= link_to "Add a restaurant", new_restaurant_path %>
